# 🎯 دليل الاستخدام - نظام البث المحدث

## ⚡ التحسينات الجديدة:

### 1️⃣ **تكبير اللوقو** ✅
- الحجم الجديد: **350 بكسل** (بدلاً من 250)
- أكبر وأوضح على الشاشة
- يحافظ على النسبة الأصلية

### 2️⃣ **وضع النسخ المباشر (Stream Copy)** ⚡ جديد!
- نسخ مباشر من المصدر بدون re-encoding
- **صفر استخدام CPU للترميز**
- **جودة أصلية 100%**
- **أسرع بكثير**
- لا يدعم اللوقو (لأنه نسخ مباشر)

---

## 🚀 كيفية الاستخدام:

### **الخيار 1: Stream Copy (موصى به للجودة والسرعة)** ⚡

```bash
# في config.sh، تأكد من:
STREAMING_MODE="copy"

# ثم شغّل:
./control.sh monitor
```

**المميزات:**
- ✅ جودة أصلية 100%
- ✅ صفر استخدام CPU
- ✅ أسرع أداء
- ❌ بدون لوقو

---

### **الخيار 2: Re-encode مع لوقو كبير** 🎨

```bash
# في config.sh، غيّر إلى:
STREAMING_MODE="encode"
LOGO_ENABLED="true"
LOGO_SIZE="350:-1"

# ثم شغّل:
./control.sh monitor
```

**المميزات:**
- ✅ لوقو كبير (350px)
- ✅ تحكم كامل في الجودة
- ⚠️ يستخدم CPU لإعادة الترميز

---

## 📊 مقارنة الوضعين:

| الميزة | Stream Copy ⚡ | Re-encode 🎨 |
|--------|---------------|--------------|
| **الجودة** | 100% أصلية | ممتازة (5000k) |
| **استخدام CPU** | 0% تقريباً | متوسط-عالي |
| **السرعة** | أسرع | عادي |
| **اللوقو** | ❌ غير متاح | ✅ 350px |
| **التحكم** | محدود | كامل |

---

## 🎬 أمثلة:

### مثال 1: البث بجودة أصلية بدون لوقو
```bash
# في config.sh
STREAMING_MODE="copy"

# شغّل
./control.sh monitor
```

### مثال 2: البث مع لوقو كبير
```bash
# في config.sh
STREAMING_MODE="encode"
LOGO_ENABLED="true"
LOGO_SIZE="350:-1"  # أو 400:-1 للوقو أكبر

# شغّل
./control.sh monitor
```

### مثال 3: تكبير اللوقو أكثر
```bash
# في config.sh
LOGO_SIZE="400:-1"  # 400 بكسل
# أو
LOGO_SIZE="500:-1"  # 500 بكسل
```

---

## 📝 ملاحظات مهمة:

1. **Stream Copy** ينسخ كل شيء من المصدر:
   - الجودة نفسها
   - الدقة نفسها (Resolution)
   - الإطارات نفسها (FPS)
   - **لا يمكن إضافة فلاتر أو لوقو**

2. **Re-encode** يعيد ترميز الفيديو:
   - تحكم كامل في الجودة
   - يمكن إضافة لوقو وفلاتر
   - يستخدم موارد CPU

3. **المصدر جودته عالية؟** → استخدم Stream Copy
4. **تريد لوقو؟** → استخدم Re-encode

---

## 🔧 إعدادات اللوقو:

في `config.sh`، يمكنك تعديل:

```bash
LOGO_SIZE="350:-1"      # الحجم (width:height)
LOGO_POSITION="topright" # الموقع
LOGO_OFFSET_X="20"      # المسافة من الحافة
LOGO_OFFSET_Y="20"      # المسافة من الأعلى
LOGO_OPACITY="0.95"     # الشفافية (0.0 - 1.0)
```

---

## ✅ التوصيات:

### للجودة القصوى والسرعة:
```bash
STREAMING_MODE="copy"
```

### للوقو مع جودة ممتازة:
```bash
STREAMING_MODE="encode"
LOGO_SIZE="350:-1"
```

---

**جرّب الآن! 🚀**

```bash
./control.sh monitor
```
