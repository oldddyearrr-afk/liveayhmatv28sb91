# 📺 Facebook Live Stream - نظام إعادة البث

## 📋 نظرة عامة

نظام متكامل لإعادة بث الفيديو من أي مصدر إلى Facebook Live باستخدام FFmpeg مع واجهة تحكم ويب عربية.

## 🗂️ بنية المشروع

### ملفات التشغيل الأساسية

#### 1. `main.sh` (محرك البث)
- السكريبت الرئيسي الذي يقوم بالبث باستخدام FFmpeg
- يفحص المتطلبات (ffmpeg, tmux)
- يتحقق من الاتصال بالإنترنت وخوادم Facebook
- يتحقق من صلاحية رابط المصدر
- يدعم وضعين:
  - **Stream Copy**: نسخ مباشر بدون إعادة ترميز (سريع جداً)
  - **Encode**: إعادة ترميز مع دعم إضافة اللوجو
- يعمل في جلسة tmux للعمل في الخلفية

#### 2. `config.sh` (ملف الإعدادات)
**الإعدادات الأساسية:**
- `SOURCE`: رابط مصدر الفيديو (HLS/DASH/RTMP)
- `RTMP_SERVER`: خادم Facebook RTMP
- `FB_STREAM_KEY`: مفتاح البث (من Replit Secrets)

**إعدادات الجودة:**
- `QUALITY_MODE`: low, medium, high, ultra, custom
- كل جودة لها إعدادات خاصة (resolution, fps, bitrate)

**إعدادات اللوجو:**
- `LOGO_ENABLED`: تفعيل/تعطيل اللوجو
- `LOGO_PATH`: مسار ملف اللوجو
- `LOGO_POSITION`: موضع اللوجو (topleft, topright, bottomleft, bottomright)
- `LOGO_SIZE`: حجم اللوجو
- `LOGO_OPACITY`: شفافية اللوجو (0.0 - 1.0)

**إعدادات متقدمة:**
- `STREAMING_MODE`: copy أو encode
- `USE_GPU`: دعم GPU للترميز (nvidia, intel, amd, off)
- `PRESET`: سرعة الترميز (ultrafast, fast, medium, slow)
- إعدادات إعادة الاتصال التلقائي

#### 3. `control.sh` (لوحة التحكم CLI)
**الأوامر المتاحة:**
```bash
./control.sh start      # بدء البث
./control.sh monitor    # بدء البث مع إعادة تشغيل تلقائي
./control.sh stop       # إيقاف البث
./control.sh restart    # إعادة تشغيل
./control.sh status     # عرض الحالة
./control.sh logs       # عرض السجلات
./control.sh attach     # الدخول للجلسة المباشرة
./control.sh help       # عرض المساعدة
```

#### 4. `web_app.py` (واجهة الويب)
- تطبيق Flask على المنفذ 5000
- API endpoints:
  - `GET /api/status`: حالة البث
  - `POST /api/start`: بدء البث
  - `POST /api/stop`: إيقاف البث
  - `GET /api/logs`: عرض السجلات
  - `GET /api/logo-config`: إعدادات اللوجو من config.sh ⭐
  - `GET /logo/<filename>`: عرض ملف اللوجو ⭐
- واجهة عربية كاملة مع تحديث تلقائي كل 3 ثواني
- معاينة مباشرة للوجو قبل البث

#### 5. `templates/index.html` (الواجهة الأمامية)
- تصميم عصري بتدرج لوني جميل
- **معاينة اللوجو المباشرة** 🎨:
  - شاشة معاينة 16:9 تحاكي البث
  - عرض اللوجو في الموضع الصحيح
  - معلومات تفصيلية عن اللوجو
  - تحديث فوري للمعاينة
- أزرار تحكم (تشغيل، إيقاف، عرض السجلات، تحديث المعاينة)
- مؤشر حالة البث (running/stopped) مع animation
- عرض معلومات البث (المدة، ملف اللوج)
- تحديث تلقائي للحالة

### ملفات إضافية

- `monitor_stream.sh`: مراقبة البث وإعادة التشغيل التلقائي
- `start_with_monitor.sh`: بدء البث مع المراقبة
- `extract_link.sh`: استخراج روابط البث من Facebook
- `channel_logo.png`: الشعار الافتراضي
- `logs/`: مجلد السجلات (log لكل جلسة بث)

## 🚀 كيفية الاستخدام

### 1. إعداد البيئة

**إضافة مفتاح البث:**
1. افتح Secrets (أيقونة القفل 🔒)
2. أضف Secret جديد:
   - Key: `FB_STREAM_KEY`
   - Value: مفتاح البث من Facebook Creator Studio

**تعديل المصدر:**
افتح `config.sh` وعدل:
```bash
SOURCE="رابط_البث_المصدر"
```

### 2. تشغيل البث

**عبر واجهة الويب:**
- شغل التطبيق: `python web_app.py`
- افتح المتصفح واضغط "تشغيل البث"

**عبر سطر الأوامر:**
```bash
./control.sh monitor    # الطريقة الموصى بها (مع إعادة تشغيل تلقائي)
./control.sh start      # الطريقة العادية
```

### 3. المراقبة والتحكم

```bash
./control.sh status     # عرض الحالة الكاملة
./control.sh logs       # عرض آخر 30 سطر من اللوج
./control.sh attach     # الدخول للجلسة المباشرة
```

## ⚙️ تخصيص الإعدادات

### تغيير الجودة
في `config.sh`:
```bash
QUALITY_MODE="ultra"    # low, medium, high, ultra
```

### تفعيل اللوجو
في `config.sh`:
```bash
LOGO_ENABLED="true"
LOGO_PATH="channel_logo.png"
LOGO_POSITION="topright"    # topleft, topright, bottomleft, bottomright
LOGO_SIZE="350:-1"           # العرض:الارتفاع (-1 للحفاظ على النسبة)
LOGO_OPACITY="0.95"          # 0.0 إلى 1.0
```

**ملاحظة:** اللوجو يعمل فقط مع `STREAMING_MODE="encode"`

### تغيير وضع البث
في `config.sh`:
```bash
STREAMING_MODE="copy"     # نسخ مباشر (سريع، بدون لوجو)
STREAMING_MODE="encode"   # إعادة ترميز (يدعم اللوجو)
```

## 🛠️ المتطلبات التقنية

### البرامج المطلوبة
- **ffmpeg**: لمعالجة الفيديو والبث
- **tmux**: لإدارة الجلسات في الخلفية
- **Python 3**: لواجهة الويب
- **Flask**: إطار عمل الويب

### المتطلبات البيئية
- اتصال إنترنت مستقر
- مفتاح بث Facebook صالح
- رابط مصدر فيديو صالح (HLS/DASH/RTMP)

## 📊 الجودات المتاحة

| الجودة | الدقة | FPS | Bitrate | الاستخدام |
|--------|-------|-----|---------|-----------|
| low | 720p | 30 | 2000k | إنترنت ضعيف |
| medium | 720p | 30 | 3000k | إنترنت متوسط |
| high | 1080p | 30 | 4500k | إنترنت جيد |
| ultra | 1080p | 30 | 5000k | إنترنت ممتاز ⭐ |

## 🔍 استكشاف الأخطاء

### البث لا يبدأ
1. تحقق من `FB_STREAM_KEY` في Secrets
2. تحقق من صلاحية رابط المصدر
3. راجع السجلات: `./control.sh logs`

### البث يتوقف فجأة
1. تحقق من الاتصال بالإنترنت
2. تحقق من صلاحية مفتاح البث
3. تحقق من أن المصدر مازال يعمل
4. استخدم `./control.sh monitor` للإعادة التلقائية

### لا تظهر السجلات
- تأكد من `LOG_ENABLED="true"` في config.sh
- تحقق من مجلد `logs/`

## 📁 الملفات الإضافية

### الوثائق
- `README.md`: الدليل الأساسي
- `HOW_TO_USE.md`: تعليمات الاستخدام التفصيلية
- `QUICK_START.md`: دليل البدء السريع
- `USAGE.md`: دليل الاستخدام
- `SOLUTION.md`: حلول المشاكل
- `FIXED.md`: التحديثات والإصلاحات

### النسخ الاحتياطية
- `config.sh.backup.20251115_011458`: نسخة احتياطية من الإعدادات

## 🎨 الميزات الخاصة

### وضع Stream Copy (النسخ المباشر)
- **السرعة**: أسرع بكثير (لا إعادة ترميز)
- **الموارد**: استهلاك CPU منخفض جداً
- **الجودة**: الجودة الأصلية بدون تغيير
- **القيود**: لا يدعم إضافة اللوجو

### وضع Encode (إعادة الترميز)
- **المرونة**: تحكم كامل في الجودة
- **اللوجو**: دعم إضافة لوجو/علامة مائية
- **الموارد**: يستهلك CPU/GPU أكثر
- **التخصيص**: تحكم كامل في كل الإعدادات

### المراقبة التلقائية
- فحص البث كل 10 ثوانٍ
- إعادة تشغيل تلقائي عند التوقف
- مثالي للبث المستمر 24/7
- سجل كامل لكل إعادة تشغيل

## 🔐 الأمان

### ممارسات آمنة
- ✅ استخدام Replit Secrets لمفتاح البث
- ✅ عدم رفع `FB_STREAM_KEY` على Git
- ✅ عدم مشاركة ملف cookies.txt

### ما يجب تجنبه
- ❌ حفظ المفاتيح في الملفات مباشرة
- ❌ مشاركة السجلات التي قد تحتوي على معلومات حساسة
- ❌ استخدام مفاتيح منتهية الصلاحية

## 📝 ملاحظات هامة

1. **رابط المصدر**:
   - يجب أن يكون البث مباشر (🔴 LIVE)
   - الروابط المستخرجة من Facebook مؤقتة
   - لكل بث جديد، استخرج رابط جديد

2. **مفتاح البث**:
   - احصل عليه من Facebook Creator Studio
   - كل صفحة لها مفتاح خاص
   - لا تشارك المفتاح مع أحد

3. **الأداء**:
   - Stream Copy أسرع بكثير
   - استخدم GPU encoding إن أمكن
   - راقب استهلاك CPU/RAM

## 🔄 آخر التحديثات

### الميزات الحديثة
- ✅ **معاينة اللوجو المباشرة** 🎨 - شاشة معاينة 16:9 تعرض موضع اللوجو بالضبط كما سيظهر في البث
- ✅ واجهة ويب عربية كاملة
- ✅ دعم المراقبة التلقائية
- ✅ دعم وضع Stream Copy
- ✅ تحسينات في الاستقرار
- ✅ سجلات منفصلة لكل جلسة
- ✅ دعم GPU encoding

### ميزة معاينة اللوجو الجديدة ⭐
- شاشة معاينة بنسبة 16:9 (مثل البث تماماً)
- عرض اللوجو في الموضع الدقيق حسب إعدادات config.sh
- معلومات تفصيلية: الموضع، الحجم، الشفافية
- زر تحديث فوري للمعاينة بعد تعديل الإعدادات
- تحديث تلقائي عند تحميل الصفحة

## 🎯 حالات الاستخدام

### مناسب لـ:
- إعادة بث محتوى من مصدر لآخر
- البث المستمر 24/7
- إضافة علامة مائية على بث موجود
- تحويل جودة البث

### غير مناسب لـ:
- البث المباشر من الكاميرا (استخدم OBS)
- تسجيل الفيديو (استخدم ffmpeg مباشرة)
- تحرير الفيديو (استخدم برامج التحرير)

---

**آخر تحديث**: نوفمبر 2025

**الحالة**: جاهز للاستخدام ✅
