# 🚀 البداية السريعة

## للبث بجودة أصلية (موصى به):

```bash
# 1. تأكد من الإعداد في config.sh
STREAMING_MODE="copy"

# 2. شغّل
./control.sh monitor
```

**النتيجة:** جودة 100% من المصدر، صفر استخدام CPU ⚡

---

## للبث مع لوقو كبير (350px):

```bash
# 1. غيّر في config.sh
STREAMING_MODE="encode"

# 2. شغّل
./control.sh monitor
```

**النتيجة:** لوقو كبير في أعلى اليمين 🎨

---

## تكبير اللوقو أكثر:

```bash
# في config.sh
LOGO_SIZE="400:-1"  # أو 500:-1
```

---

**ملاحظة:** الوضع الافتراضي الآن هو `copy` (جودة أصلية)
