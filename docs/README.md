# 📺 Facebook Live Stream - نظام إعادة البث

نظام بسيط لإعادة بث الفيديو من مصدر إلى Facebook Live

---

## 📁 الملفات الأساسية

### ملفات التشغيل:
- **main.sh** - السكريبت الرئيسي للبث
- **control.sh** - لوحة التحكم (بدء/إيقاف/حالة البث)
- **config.sh** - ملف الإعدادات (الجودة، الرابط، الخ)

### ملفات المساعدة:
- **extract_link.sh** - استخراج رابط البث من فيسبوك
- **logo.png** - الشعار (اختياري)

---

## 🚀 طريقة الاستخدام

### الخطوة 1: تعديل رابط المصدر في config.sh

افتح ملف `config.sh` وعدل:
```bash
SOURCE="رابط_البث_المصدر_هنا"
```

---

### الخطوة 2: إضافة مفتاح البث في Replit Secrets

1. افتح تبويب **Secrets** في Replit (🔒 أيقونة القفل)
2. أضف Secret جديد:
   - **Key**: `FB_STREAM_KEY`
   - **Value**: مفتاح البث من Facebook

احصل على المفتاح من:
- Facebook Creator Studio
- أو صفحة البث المباشر

---

### الخطوة 3: بدء البث

#### ⭐ الطريقة الموصى بها (مع المراقبة التلقائية):
```bash
./control.sh monitor
```
✅ **يعيد تشغيل البث تلقائياً عند التوقف**

#### الطريقة العادية:
```bash
./control.sh start
```

أو يمكنك تمرير المفتاح مباشرة:
```bash
./control.sh start YOUR_STREAM_KEY_HERE
```

---

## 🎮 أوامر التحكم

```bash
./control.sh monitor    # بدء البث مع إعادة تشغيل تلقائي ⭐
./control.sh start      # بدء البث (عادي)
./control.sh stop       # إيقاف البث
./control.sh restart    # إعادة تشغيل
./control.sh status     # عرض الحالة
./control.sh logs       # عرض السجلات
./control.sh attach     # الدخول للجلسة المباشرة
```

### ⭐ ميزة المراقبة التلقائية الجديدة:
- **يفحص البث كل 10 ثوانٍ**
- **يعيد التشغيل تلقائياً عند التوقف**
- **مثالي للبث المستمر 24/7**

---

## ⚙️ الإعدادات (config.sh)

### تغيير الجودة:

```bash
QUALITY_MODE="ultra"    # ultra, high, medium, low
```

### الجودات المتاحة:
- **low**: 720p @ 30fps (2000k bitrate)
- **medium**: 720p @ 30fps (3000k bitrate)
- **high**: 1080p @ 30fps (4500k bitrate)
- **ultra**: 1080p @ 30fps (5000k bitrate) ⭐

---

## 📋 ملف link.json

بعد استخراج الرابط، يتم حفظ المعلومات في `link.json`:

```json
{
  "extracted_at": "2025-01-15 12:00:00",
  "facebook_url": "https://www.facebook.com/share/v/xxxxx/",
  "stream_url": "https://scontent...mpd",
  "format": "DASH (MPD)",
  "status": "active"
}
```

---

## 🔒 الأمان

- **لا تشارك** ملف الكوكيز مع أحد
- **احذف** ملف الكوكيز بعد الاستخدام
- **لا ترفع** FB_STREAM_KEY على Git

---

## ⚠️ ملاحظات مهمة

1. **البث يجب أن يكون مباشر (🔴 LIVE)** عند استخراج الرابط
2. الرابط المستخرج **مؤقت** وينتهي بعد انتهاء البث
3. لكل بث جديد، **استخرج رابط جديد**
4. تأكد من وجود اتصال إنترنت **قوي ومستقر**

---

## 🛠️ المتطلبات

- **ffmpeg** - لمعالجة الفيديو
- **tmux** - لإدارة الجلسات
- **yt-dlp** - لاستخراج الروابط (يثبت تلقائياً)
- **Python 3** - لتشغيل yt-dlp

---

## 📞 المساعدة

إذا واجهت مشكلة:

```bash
# عرض سجلات البث
./control.sh logs

# التحقق من حالة البث
./control.sh status

# الدخول للجلسة المباشرة
./control.sh attach
```

---

**بالتوفيق! 🚀**
